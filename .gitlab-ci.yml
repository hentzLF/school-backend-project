stages:
  - deploy

deploy:
  stage: deploy
  only:
    - main
  tags:
    - csharp-backend
  script:
    - docker network create agrimarket-net 2>/dev/null || true
    - docker compose up --build --remove-orphans --detach
